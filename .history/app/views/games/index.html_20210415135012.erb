<p id="notice"><%= notice %></p>

<h1 class = "main-text">Games</h1>

<table>
  <thead>
    <tr>
      <th class = "main-text">Title</th>
      <th class = "main-text">Cost</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @current_user.games.each do |game| %>
      <tr>
        <td class = "main-text"><%= game.name %></td>
        <td><%= game.amount %></td>
        <td><%= link_to 'Show', user_game_path(current_user, game.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add New', new_user_game_path(current_user) %>
